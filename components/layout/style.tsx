import styled from "styled-components";

const PageLayoutContainer = styled.div`
  width: 100%;
  display: flex;
  justify-content: center;
`;

const PageLayoutBody = styled.div`
  width: 1200px;
`;

export { PageLayoutContainer, PageLayoutBody };
